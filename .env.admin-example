# Admin Account Configuration
# Add this to your .env file to enable the admin account

# Admin password for unlimited access account
ADMIN_PASSWORD=your_secure_admin_password_here

# Admin login credentials:
# Email: admin@explainthis.app
# Password: [use the same password as ADMIN_PASSWORD above]
# Tier: admin (unlimited ticker access)

# SECURITY NOTE: Never commit the actual password to version control!
# This is just an example file. Set your real password in .env (which is gitignored)
